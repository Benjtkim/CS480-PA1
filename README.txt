Name: Benjamin Kim
Collaborators: None
Class: CS 480
Assignment Number: PA1
Summary: For drawLine() and implementing Bresenham's algorithm, the easiest case to implement was drawing a line that has a shallow slope and positive delta_x and y. For that, I simply applied what we learned in class, writing the function to iterate from (p1.x+1, p1.y/p1.y+1) to (p2.x, p2.y) while using the variable D_curr (initialized to 2 * delta_y - delta_x) to decide whether to pick the current or upper y-coordinate. For the next iteration, my code preemptively updates D_curr based on whether it's greater than 0 or not. In case of the former, it adds 2 *delta_y - 2 * delta_x to D_curr and increments (or decrements) Y_curr (initialized to 0), and in case of the latter, it adds only 2 * delta_y to D_curr and doesn't increment (or decrement) Y_curr. It got trickier when coding the algorithm for steep and/or negative slopes, and I additionally had to consider if a negative slope was a result of a negative delta_x or y. These factors are why I have 8 cases in my drawLine() function. Each case has a comment explaining what I did and why I did it that way, but I'll explain in broad strokes here. For steep slopes, the hint I received from lab to consider that lines with steep slopes become ones with shallow slopes when reflected across y=x was very helpful, as it allowed me to code an implementation that calculates the points that would make up the line for the reciprocal slope, but plots them with their coordinates flipped. As for lines with negative delta_xs, I relied on iterating backwards on x, and for lines with negative delta_ys, I relied on choosing the current or lower y-coordinate by decrementing y_curr with yStep, which is set to -1 depending on the case. Finally, for drawTriangle(), I implemented the horizontal scan-line algorithm we learned in class, and I made sure to split the triangle into 2 based on the middle-coordinate, which I found by adding p1, p2, and p3 into a list and sorting them by their y-coordinates. The drawTriangle() function first draws the 3 edges, saving the list each call to drawLine() generates to a variable, before sorting them in descending order of y since each scanline must get lower and lower. Then, it groups together the generated points by their y-coordinate using code that assigns the points to their dedicated key in a dictionary. After that, it determines the lines needed to interpolate from and therefore the required dictionaries based on which point is the upper point and which point is the middle point. Finally, it applies the triangle rasterization method we learned in class by fetching the two points on either side from the required dictionary, and interpolating from there. The reason there are 2 for loops is that this interpolation must be done twice as the method specifies.